{
  "command": "python3 -m mypy src",
  "setupCommand": "python3 -m pip install -r requirements.txt",
  "category": "python_mypy_optional_return",
  "difficulty": "project-level",
  "expectedRagIds": ["playbook_python_mypy_optional_return"],
  "expectedChangedFiles": ["src/accounts/service.py"],
  "expectedEvidence": {
    "rootCause": "optional value returned where str is required",
    "concepts": ["mypy", "optional", "return-value", "display_name"],
    "strategies": ["provide a typed fallback", "narrow None before returning"],
    "failureTypes": ["typecheck_error"],
    "errorCodes": ["mypy:return-value"]
  }
}
